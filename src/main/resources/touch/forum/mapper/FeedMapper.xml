<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="touch.forum.mapper.FeedMapper">

    <resultMap id="feedMap" type="touch.forum.entity.Feed">
        <result property="createAt" column="create_at"/>
    </resultMap>


    <insert id="createFeed" parameterType="touch.forum.entity.Feed" useGeneratedKeys="true" keyProperty="id">
        insert into feed values (#{id},#{type},#{userId},#{createAt},#{data})
    </insert>

    <select id="selectUserFeeds" resultMap="feedMap" resultType="touch.forum.entity.Feed">
        select * from feed where user_id in
        <foreach collection="userId" index = "index" item = "item"
                 open = "(" separator="," close=")">
            #{item}
        </foreach>
        order by id desc
    </select>

    <select id="getFeedbyId" resultMap="feedMap" resultType="touch.forum.entity.Feed">
        select * from feed where id = #{id}
    </select>

</mapper>