server:
  servlet:
    context-path: /forum
    session:
      cookie:
        path: /
  port: 8000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/forum?characterEncoding=utf-8&useSSL=false&serverTimezone=Australia/Sydney
    username: root
    password: mobile123
  redis:
    host: localhost
    port: 6379
    database: 0

mybatis:
  mapper-locations: classpath:touch/forum/mapper/*.xml
  type-aliases-package: touch.forum.entity